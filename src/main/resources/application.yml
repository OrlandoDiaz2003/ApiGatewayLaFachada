spring:
  cloud:
    gateway:
      mvc:
        cors-configurations:
          '[/**]':
            allowed-origins: "http://localhost:puerto_frontend"
            allowed-methods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowed-headers: "*"
            allow-credentials: true
            max-age: 3600